{
  "name": "shiplog",
  "version": "1.0.0",
  "description": "Daily snapshots of your forge activity into your own Postgres.",
  "private": true,
  "module": "bin/collect.ts",
  "type": "module",
  "scripts": {
    "init": "bun bin/init.ts",
    "collect": "bun bin/collect.ts",
    "render": "bun bin/render.ts",
    "migrate": "dbmate up",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.13",
    "@types/pg": "^8.11.10",
    "pg-mem": "^3.0.4",
    "typescript": "^5.6.3"
  }
}

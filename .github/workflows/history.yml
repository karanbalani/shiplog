name: history

on:
  schedule:
    - cron: '41 */2 * * *'
  workflow_dispatch:
    inputs:
      mode:
        description: 'History mode: fast for authored commits, deep for co-authored/private contribution repair'
        required: false
        default: 'deep'
        type: choice
        options:
          - fast
          - deep
      max_minutes:
        description: 'Maximum run time before pausing between repositories'
        required: false
        default: '30'
        type: string
      repo_budget_minutes:
        description: 'Time budget reserved before starting another repository'
        required: false
        default: '5'
        type: string

permissions:
  contents: read

concurrency:
  group: shiplog-ingestion-${{ github.repository }}
  cancel-in-progress: false

jobs:
  history:
    runs-on: ubuntu-latest
    steps:
      - id: checkout
        uses: actions/checkout@v6

      - id: setup_bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dbmate
        id: install_dbmate
        run: |
          curl -fsSL -o /usr/local/bin/dbmate https://github.com/amacneil/dbmate/releases/latest/download/dbmate-linux-amd64
          chmod +x /usr/local/bin/dbmate

      - name: Install dependencies
        id: install_dependencies
        run: bun install --frozen-lockfile

      - name: Write shiplog config
        id: write_config
        run: |
          if [ -z "${SHIPLOG_CONFIG_BASE64:-}" ]; then
            echo "missing repository variable: SHIPLOG_CONFIG_BASE64" >&2
            exit 1
          fi
          printf '%s' "$SHIPLOG_CONFIG_BASE64" | base64 -d > shiplog.config.json
          bun -e "import { load } from './lib/config.ts'; load()"
        env:
          SHIPLOG_CONFIG_BASE64: ${{ vars.SHIPLOG_CONFIG_BASE64 }}

      - name: Export read token env
        id: export_read_tokens
        run: bun run tokens:export -- --scope read
        env:
          SHIPLOG_TOKEN_SECRETS_JSON: ${{ toJSON(secrets) }}

      - name: Wait for database
        id: wait_database
        run: bun run db:wait
        env:
          DATABASE_CONNECTION_STRING: ${{ secrets.DATABASE_CONNECTION_STRING }}

      - name: Run migrations
        id: run_migrations
        run: bun run migrate
        env:
          DATABASE_CONNECTION_STRING: ${{ secrets.DATABASE_CONNECTION_STRING }}

      - name: Initialize accounts
        id: initialize_accounts
        run: bun run init
        env:
          DATABASE_CONNECTION_STRING: ${{ secrets.DATABASE_CONNECTION_STRING }}

      - name: Improve historical activity
        id: backfill_history
        run: bun run backfill
        env:
          DATABASE_CONNECTION_STRING: ${{ secrets.DATABASE_CONNECTION_STRING }}
          BACKFILL_MODE: ${{ github.event.inputs.mode || 'deep' }}
          BACKFILL_MAX_MINUTES: ${{ github.event.inputs.max_minutes || '30' }}
          BACKFILL_REPO_BUDGET_MINUTES: ${{ github.event.inputs.repo_budget_minutes || '5' }}

      - name: Write run summary
        id: write_summary
        if: ${{ always() }}
        continue-on-error: true
        run: |
          if command -v bun >/dev/null 2>&1 && [ -f scripts/write_workflow_summary.ts ]; then
            bun scripts/write_workflow_summary.ts
          elif [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            failed_phase=bootstrap
            if [ "${SHIPLOG_CHECKOUT_OUTCOME:-}" != success ]; then
              failed_phase=checkout
            elif [ "${SHIPLOG_SETUP_BUN_OUTCOME:-}" != success ]; then
              failed_phase=setup_bun
            fi
            printf '## Shiplog · %s\n\n**Result:** ❌ Failed\n**Failed phase:** `%s`\n\nError code: [`SHIPLOG-UNKNOWN-001`](%s#shiplog-unknown-001)\n\n### How to fix\n\nReview the failed bootstrap step and job log, correct the setup problem, then rerun.\n' \
              "$SHIPLOG_WORKFLOW_NAME" "$failed_phase" "$SHIPLOG_ERROR_DOCS_URL" >> "$GITHUB_STEP_SUMMARY"
          fi
        env:
          SHIPLOG_WORKFLOW_NAME: ${{ github.workflow }}
          SHIPLOG_JOB_NAME: ${{ github.job }}
          SHIPLOG_JOB_STATUS: ${{ job.status }}
          SHIPLOG_ERROR_DOCS_URL: ${{ github.server_url }}/${{ github.repository }}/blob/${{ github.sha }}/docs/ERRORS.md
          SHIPLOG_CHECKOUT_OUTCOME: ${{ steps.checkout.outcome }}
          SHIPLOG_SETUP_BUN_OUTCOME: ${{ steps.setup_bun.outcome }}
          SHIPLOG_STEPS_JSON: >-
            {"checkout":"${{ steps.checkout.outcome }}","setup_bun":"${{ steps.setup_bun.outcome }}","install_dbmate":"${{ steps.install_dbmate.outcome }}","install_dependencies":"${{ steps.install_dependencies.outcome }}","write_config":"${{ steps.write_config.outcome }}","export_read_tokens":"${{ steps.export_read_tokens.outcome }}","wait_database":"${{ steps.wait_database.outcome }}","run_migrations":"${{ steps.run_migrations.outcome }}","initialize_accounts":"${{ steps.initialize_accounts.outcome }}","backfill_history":"${{ steps.backfill_history.outcome }}"}
